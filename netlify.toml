[build]
  publish = "."
  command = "echo 'Static site, no build command needed'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Disable Lighthouse plugin to avoid network issues
[build.environment]
  NETLIFY_PLUGIN_LIGHOUSE = "false"
